# SOUL: DVA의 설계 철학

이 문서는 DVA가 기능과 실행 모델을 선택할 때 따르는 변하지 않는 판단 기준이다.

## 무엇인가

**DVA(Dev Virtual Auto)**는 서로 다른 개발 실행 도구를 하나의 선언적 워크플로우로
조정하는 로컬 우선 개발 환경 오케스트레이터다.

사용자는 실행 도구의 세부 명령을 반복해서 조합하는 대신, 이름 있는 실행 의도를
선택하고 DVA는 그 의도를 예측 가능한 계획으로 해석한다.

## 왜 만드는가

현실의 개발 환경은 컨테이너, 클러스터, 로컬 프로세스와 초기화 작업이 함께 존재한다.
도구마다 명령과 설정 방식이 다르면 실행 지식이 개인의 기억과 셸 기록에 흩어지고,
같은 프로젝트도 사람과 장소에 따라 다르게 실행된다.

DVA는 선언, 선택, 실행의 책임을 분리한다. 프로젝트가 실행 가능한 대상을 선언하고,
사용자는 의도를 선택하며, 런타임은 검증된 계획만 실행한다.

## 핵심 신념 5가지

backend와 명령 표면이 바뀌어도 다음 다섯 가지 원칙은 유지한다.

### 1. 선언과 실행 계획을 분리한다

- 재사용 가능한 대상 정의와 이번 실행의 선택을 섞지 않는다.
- 사용자가 실행하는 것은 내부 구현 조각이 아니라 이름 있는 의도다.
- 환경 차이는 선언을 복제하지 않고 계획을 해석할 때 적용한다.

### 2. 편의보다 예측 가능성을 우선한다

- 실행 전에 최종 계획을 해석하고 검증할 수 있어야 한다.
- 같은 설정과 입력은 같은 실행 순서를 만들어야 한다.
- 암묵적인 추측보다 명시적인 선택과 오류를 선호한다.

### 3. 하나의 동작에는 하나의 소유자만 둔다

- 외부 도구는 자신의 리소스와 의존 관계를 소유한다.
- DVA는 도구를 대체하지 않고 선택, 순서, 수명 주기를 조정한다.
- 같은 서비스나 검사를 여러 계층에서 중복 관리하지 않는다.

### 4. 환경과 실행 위치를 독립적으로 다룬다

- 용도(개발·테스트·유지보수)와 실행 위치(local·remote)는 다른 축이다.
- 실행 위치는 로컬을 기본으로 두고, 원격은 로컬에서 재현할 수 없는 리소스에만 쓴다.
- 한 축의 변화가 다른 축의 선언 복제를 요구하지 않아야 한다.
- 비밀값과 로컬 차이는 공유 선언에서 분리한다.

### 5. 사람과 에이전트가 같은 인터페이스를 사용한다

- 사용 가능한 명령과 최종 설정은 기계가 읽을 수 있어야 한다.
- 에이전트는 명령을 추측하지 않고 프로젝트가 제공하는 정보를 조회한다.
- 자동화도 사람이 검토할 수 있는 실행 계획과 결과를 남긴다.

## 이 신념 속의 선택

| 상황 | 무엇을 우선하는가 | 이유 |
|------|------------------|------|
| 단축 명령 vs 명시적 계획 | 명시적 계획 | 실행 결과를 설명하고 재현할 수 있어야 한다 |
| 기능 통합 vs 도구 대체 | 기능 통합 | 기존 생태계의 책임과 강점을 유지한다 |
| 자동 탐지 vs 선언 | 검증 가능한 선언 | 환경마다 달라지는 추측을 줄인다 |
| 설정 복제 vs 합성 | 합성 | 공통 의도를 유지하면서 차이만 표현한다 |
| 자동 수정 vs 사용자 통제 | 진단 우선 | 실행 환경을 바꾸는 결정은 드러나야 한다 |

## 철학의 한 줄

> **한 번 선언하고, 이름으로 선택하며, 검증된 계획만 실행한다.**
