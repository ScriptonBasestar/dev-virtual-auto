version: "0.1.22"

# Deliberately uses the legacy stack.<entry>.compose shape, which both schema.json and
# the loader reject (553c478). This fixture exists so the rejection stays covered from
# the outside — see TestLegacyComposeFixtureIsRejected. It must keep failing to load; do
# not migrate it to runners.compose, and do not add it to the validate tests.
stack:
  compose:
    order: 10
    files: [compose.yml]
    project_name: legacy-test
