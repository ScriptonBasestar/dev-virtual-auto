version: "0.1.22"

# Deliberately uses the legacy stack.<entry>.compose shape, which schema.json
# rejects. The loader still supports it for back-compat, so this fixture exists
# only to keep that path covered. Do not add it to the validate tests.
stack:
  compose:
    order: 10
    files: [compose.yml]
    project_name: legacy-test
