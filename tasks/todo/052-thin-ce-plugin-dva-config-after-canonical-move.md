---
id: TASK-052
title: "Remove dva-config content from claude-ce-plugin; keep only a reference to the dva plugin"
type: chore
priority: P2
status: todo
effort: S
created-at: 2026-07-22T00:00:00+09:00
scope: "Cross-repo: claude-ce-plugin (must be applied from a ce-plugin-scoped session)"
---

# Task 052: Make ce-plugin reference-only for DVA config

## Decision (settled)

The `dva:config` skill content lives **only** in the dva repo
(`claude-plugin/skills/config/`). `claude-ce-plugin` keeps a lightweight
**reference/pointer** and ships **no** dva-config prompt/skill content — no dual
management.

Trade-off explicitly accepted: `tool:dva-config` was globally installed (auto-
triggered in any session, including consumer/devbox projects where `dva.yml` is
authored). After removal, the config skill is only available where the dva plugin
is loaded. Single-source-of-truth was chosen over that convenience. If global
availability is later wanted, install/publish the dva plugin globally — do NOT
re-embed content in ce-plugin.

## Why this couldn't be done from the dva-repo session

Cross-repo writes to `~/mywork/scripton/claude-ce-plugin` are blocked by this
session's permission boundary (project-dir scope). Apply the change-set below
from a session scoped to the ce-plugin repo.

## Change-set (claude-ce-plugin)

Repo: `~/mywork/scripton/claude-ce-plugin` (source of truth = `src/`; `plugins/`,
`.opencode/`, `_agents/`, `build/codex/` are generated by `./build/ce`).

1. **Delete the source skill**
   - `rm -rf src/plugins/tool/skills/dva-config/`  (SKILL.md, README.md)

2. **`src/plugins/tool/CLAUDE.md`** — remove all 4 structured references:
   - Quick Skill Lookup selector row: `| DVA, dva.yml, config validate/doctor, schema migration | \`dva-config\` | high |`
   - Components table row: `| \`dva-config\` | DVA configuration create/audit/migrate/diagnose workflow |`
   - Skill Composition block: the `# DVA config …` comment + `dva-config:` + its `chains-with:` line
   - Directory Structure: the `└── dva-config/` line; change the now-last
     `├── shellforge-validator/` to `└── shellforge-validator/`
   - Add a **prose** pointer (NOT a `chains-with`/selector ref, or `ce validate
     skillref` will flag it as dangling): e.g. "DVA 설정(dva.yml) 저작·진단은 이
     플러그인에서 제거되었습니다. DVA 리포의 dva 플러그인이 canonical 소스입니다
     (2중 관리 방지)."

3. **`src/plugins/INDEX.md`** — remove the tool-section row
   `| dva-config | DVA 설정 생성·감사·마이그레이션·진단 |` (line ~200). Optional
   one-line prose pointer to the dva plugin.

4. **Validate**: `./build/ce validate skillref src/plugins` must exit 0. The
   validator resolves cross-plugin refs against the full catalog (95 skills), so
   any remaining structured `dva-config`/`dva:config` ref will fail — keep the
   pointer as plain prose. Baseline before edits was `✅ No issues found`.

5. **Regenerate + refresh** so no generated mirror keeps a stale copy:
   - `make build-plugins`  (regenerates `plugins/`, `.opencode/`, `_agents/`,
     `build/codex/` — confirm the diff only removes dva-config, no unrelated churn)
   - `make install-claude`  (or `rm -rf ~/.claude/plugins/cache/claude-ce-plugin/`)
     to drop the stale global cache copy.
   - Generated dva-config mirrors to verify gone:
     `plugins/tool/skills/dva-config/`, `.opencode/skills/tool-dva-config/`,
     `_agents/skills/tool-dva-config/`, `build/codex/skills/tool-dva-config/`.

6. **Commit** (no push): `refactor(tool): drop dva-config skill; reference dva plugin as canonical`.

## Acceptance criteria

- [ ] No `dva-config`/`tool-dva-config` skill dir under ce-plugin `src/` or any generated tree.
- [ ] `ce validate skillref src/plugins` exits 0 (no dangling refs).
- [ ] ce-plugin docs contain only a prose pointer to the dva plugin, no skill content.
- [ ] Global cache no longer serves `tool:dva-config`.
- [ ] dva repo `claude-plugin/skills/config/` remains the sole source of truth.

## Provenance

- Port into dva repo: commit `fb945d8` (this repo), from ce-plugin source `dff2de3`.
- Inbound `chains-with` to dva-config from other ce-plugin skills: NONE — removal
  breaks no skill dependency graph.
